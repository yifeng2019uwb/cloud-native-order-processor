# AWS credentials are no longer needed - using STS role assumption
# The role ARN is provided via environment variable AWS_ROLE_ARN
---
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: order-processor
  labels:
    app: order-processor
    component: secrets
    environment: local
type: Opaque
data:
  # echo -n "your-jwt-secret" | base64
  jwt-secret: bG9jYWwtZGV2LXNlY3JldC1rZXk=