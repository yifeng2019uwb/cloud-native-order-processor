"""
Inventory Service Business Validators

Provides business logic validation for the inventory service.
Only validates API request fields (GET endpoints only).
"""

# No business validators needed for current inventory service APIs
# The service only has GET endpoints that don't require business logic validation
# Asset existence is handled by the database layer returning None/empty results