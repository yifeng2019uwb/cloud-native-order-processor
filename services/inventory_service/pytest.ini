[pytest]
# Note: Run tests via ./dev.sh test (not raw pytest) to ensure the service virtual environment is activated
# The dev.sh script activates .venv-inventory_service with all required dependencies
addopts = --cov --cov-report=term-missing --cov-fail-under=80