# User Service Requirements
# Path: services/user_service/requirements.txt

# FastAPI and ASGI server
# fastapi is provided by common/requirements.txt (>=0.110.0)
uvicorn==0.35.0

# Pydantic for data validation
# pydantic is provided by common/requirements.txt (>=2.8.0)

# AWS SDK for DynamoDB
# boto3 is provided by common/requirements.txt (>=1.34.0)

# JWT and authentication
python-jose[cryptography]==3.5.0
PyJWT==2.8.0

# Password hashing
# bcrypt is provided by common/requirements.txt (>=4.1.0)

# Form data handling
python-multipart==0.0.6

# python-dotenv, email-validator, pyyaml, redis are provided by common/requirements.txt

# AWS Lambda handler (for future serverless deployment)
mangum==0.17.0

# Metrics and monitoring
prometheus-client==0.19.0

# Test dependencies
# pytest, pytest-asyncio, pytest-cov are provided by common/requirements.txt
# (pytest>=8.0.0, pytest-asyncio>=0.23.0, pytest-cov>=4.1.0)
httpx>=0.25.0
